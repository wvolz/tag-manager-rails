<% content_for :title, "Edit Company Prefix Map" %>

<div class="max-w-2xl space-y-6">
  <div>
    <h1 class="text-2xl font-bold">Edit Company Prefix Map</h1>
    <p class="mt-1 text-sm text-base-content/60">Update the local company-prefix mapping used by the decoder.</p>
  </div>

  <%= render "form", company_prefix_map: @company_prefix_map %>
</div>
