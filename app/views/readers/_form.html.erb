<%= form_with(model: reader) do |form| %>
  <% if reader.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reader.errors.count, "error") %> prohibited this reader from being saved:</h2>

      <ul>
        <% reader.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mt-1">
    <%= form.label :name, style: "display: block", :class=>'label' %>
    <%= form.text_field :name, :class=>'input' %>
  </div>

  <div class="mt-1">
    <%= form.label :location, style: "display: block", :class=>'label' %>
    <%= form.text_field :location, :class=>'input' %>
  </div>

  <div class="mt-1">
    <%= form.submit class:"btn btn-primary" %>
  </div>
<% end %>
