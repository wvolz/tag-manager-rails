<%= form_with(model: tag_type, local: true) do |form| %>
  <% if tag_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tag_type.errors.count, "error") %> prohibited this tag_type from being saved:</h2>

      <ul>
      <% tag_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mt-1">
    <%= form.label :name, :class=>'label' %>
    <%= form.text_field :name, id: :tag_type_name, :class=>'input' %>
  </div>

  <div class="mt-1">
    <%= form.label :decoder, :class=>'label' %>
    <%= form.select :decoder, TagType.decoders.keys, {}, {class: "select"} %>
  </div>

  <div class="mt-1">
    <%= form.submit class:"btn btn-primary" %>
  </div>
<% end %>
